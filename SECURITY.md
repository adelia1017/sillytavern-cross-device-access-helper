# 安全策略

## 安全承诺

本项目的主扩展默认是前端安全命令生成器，无遥测、无外部网络请求。它只会请求当前 SillyTavern 的固定同源路径，以检测用户是否主动安装了配套后端并生成修改预览。项目不会要求关闭白名单、CSRF、认证或其他 SillyTavern 安全功能。

配套后端是完全可选、独立开源的服务器组件。服务器插件没有沙箱，会继承 SillyTavern Node.js 进程权限。主扩展不会自动安装、执行或开启它；未安装后端时，完整安全模式始终可用。

## 报告安全问题

公开仓库创建后，请通过 GitHub Security Advisory 私下报告以下问题：

- 可生成超出预定私有 IPv4 或 `/24` 范围的命令
- 修改 `listen`、`whitelistMode`、`whitelist` 之外的配置语义
- 失败后覆盖或破坏原 `config.yaml`
- 泄露用户输入、酒馆数据或密钥
- 向当前 SillyTavern 固定同源接口之外发起网络请求，或发起模型调用、遥测和未经确认的配置修改

一般使用问题可以提交公开 Issue，但请先遮住 IP、密钥、用户名和文件内容中的私人信息。

## 不在支持范围内

以下场景不属于第一版安全承诺：

- 用户自行修改生成的命令
- 非 `~/SillyTavern` 路径
- 非 Android Termux 环境
- 公网暴露、反向代理、VPN、容器或自定义网络结构
- 与其他脚本同时修改 `config.yaml`
- 手动删除或修改本助手创建的备份
