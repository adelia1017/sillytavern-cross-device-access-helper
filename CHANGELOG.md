# 更新日志

## 0.4.0 - 2026-08-14

- 安全配置向导新增 Android Termux / Windows 10/11 服务器系统选择，选择不会保存。
- Windows 模式生成可整段复制的 PowerShell 命令，只处理当前 SillyTavern 文件夹中的 `config.yaml`。
- Windows 命令沿用重复键拒绝、允许字段限制、临时文件验证、`fsync`、时间戳备份和恢复前安全备份流程。
- 新增 Windows 停止酒馆、从安装目录打开 PowerShell、使用 `Start.bat` 启动、查找 IPv4 和防火墙专用网络教程。
- 修正 Windows 上需要以可写句柄执行 `fsync` 的跨平台差异。
- 可选后端支持范围不变，仍只支持 Android Termux。

## 0.3.3 - 2026-08-12

- 将安全配置向导和后端功能的折叠标题改为醒目的大按钮样式。
- 增加独立配色、按压反馈、键盘焦点及“点击展开/收起”提示。
- 优化窄屏按钮布局，避免状态标签与展开提示互相挤压。

## 0.3.2 - 2026-08-12

- 新增服务器设备使用手机流量并开启热点时的折叠教程。
- 明确访问设备 IP 用于白名单，热点的路由器/默认网关用于访问网址。
- 明确手机流量公网 IP 不适用于本地跨设备访问。

## 0.3.1 - 2026-08-12

- 统一使用“服务器设备”和“访问设备”，并在面板开头解释两者区别。
- 后端状态新增可折叠的当前白名单列表。
- 后端区域新增可折叠的访问设备 IPv4 查找教程。
- 移除个人快捷命令 `st`，统一采用官方 Termux 启动方式 `bash start.sh`。
- 安装、配置和恢复后的启动命令均可点按复制。

## 0.3.0 - 2026-08-12

- 完成可选后端的预览、二次确认、安全应用和恢复最近备份流程。
- 只有有效预览且后端允许写入时才显示应用按钮；更改 IP 或模式会立即作废旧预览。
- 应用成功后显示备份名、Termux 手动重启步骤和“本次运行仍使用旧配置”提醒。
- 恢复操作不接受用户路径，恢复前会再次备份当前配置。
- 推荐访问网址可选生成本地二维码，不连接外部二维码服务。
- 新增完整前端交互、严格请求体、CSRF 和零外部请求回归测试。

## 0.2.2 - 2026-08-12

- 根据访问设备 IPv4 自动推荐同一 `/24` Wi-Fi 网段中的手机访问网址。
- 推荐网址单独显示并提供复制按钮。
- VPN、移动网络和其他虚拟网络地址默认收进“其他检测到的地址（通常不用）”。
- 未填写访问设备 IP 时不擅自选择，直接提示用户先填写。

## 0.2.1 - 2026-08-12

- 安全向导与可选后端改为两个互不切换、互不隐藏的折叠区。
- 安全向导默认展开但可以收起；后端功能默认收起。
- 只有首次展开后端区或点击“重新检查后端”时才请求一次状态，不后台轮询。
- 修正文案，直接说明当前后端只提供状态读取和修改预览。
- 后端未连接时显示 HTTP 状态或网络失败原因，不再只显示模糊结论。

## 0.2.0-dev.2 - 2026-08-12

- 页面默认稳定停留在安全命令模式，不自动检测或后台轮询后端。
- 新增“我已安装好，切换到后端模式”按钮，只在用户点击时检测一次。
- 区分“后端未连接”和“后端已连接但配置检查失败”，并直接显示后端返回的安全错误。

## 0.2.0-dev.1 - 2026-08-12

- 将安全命令模式和可选后端入口整合进同一个前端扩展。
- 未安装后端时默认显示完整安全模式，并提供醒目的后端安装按钮。
- 检测到后端时自动切换为状态检查和修改预览界面。
- 后端不可用时自动回退，不保存用户的模式选择或 IP。
- 后端仓库改为纯服务器组件，不再提供第二个前端扩展。

所有重要变更都会记录在此文件中。

## [0.1.1] - 开发中

### 改进

- 将技术型命令生成器重做为四步新手向导。
- 内置 iPad、Windows 和 Android 查询局域网 IP 的图形教程。
- 支持填写安卓手机 IP 后生成并复制访问网址。
- 当页面通过私有局域网 IPv4 打开时，自动识别服务器 IP。
- 默认折叠 `/24`、YAML 和原始长命令等技术细节。

## [0.1.0] - 2026-08-12

### 新增

- 创建纯前端扩展基础骨架。
- 添加扩展清单、设置面板和安全边界说明。
- 添加客户端 IPv4 即时校验和私有地址范围限制。
- 添加单设备与同一 `/24` 可信局域网模式预览。
- 添加 IP 边界与隐私边界自动化测试。
- 添加安全修改、失败保护与备份恢复的隔离集成测试。
- 添加安装、使用、卸载、恢复、隐私、安全与风险文档。
- 添加 GitHub 自动测试工作流和脱敏 Bug 报告模板。
- 修复部分移动端主题中按钮被拉高、文字竖排的问题。
